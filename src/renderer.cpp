#include "renderer.h"

void Renderer::init() {
    // Initialize shaders, load models and textures
}

void Renderer::render(Map* map) {
    // Render the map
}

void Renderer::render(Player* player) {
    // Render the player
}

void Renderer::render(Gun* gun) {
    // Render the gun
}

void Renderer::render(Minimap* minimap) {
    // Render the minimap
}
